server.port: 8082
server.error.include-message: always

logging:
  level:
    root: INFO
    team.caltech.olmago.customer.domain: DEBUG
    com:
      querydsl:
        sql: DEBUG
    org:
      hibernate:
        SQL: DEBUG
        type: DEBUG
