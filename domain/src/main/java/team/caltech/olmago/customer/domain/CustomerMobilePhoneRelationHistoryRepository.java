package team.caltech.olmago.customer.domain;

import org.springframework.data.jpa.repository.JpaRepository;

public interface CustomerMobilePhoneRelationHistoryRepository extends JpaRepository<CustomerMobilePhoneRelationHistory, Long> {
}
